// Main.dart
import 'package:flutter/material.dart';

void main() {
  runApp(TiktokCloneApp());
}

class TiktokCloneApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'TikTok Clone',
      home: VideoFeedScreen(),
    );
  }
}

// VideoFeedScreen.dart
class VideoFeedScreen extends StatelessWidget {
  // Replace this with Firebase video fetch logic
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Video feed UI
    );
  }
}
